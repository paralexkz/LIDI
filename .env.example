# LLM provider key — ScrapeGraphAI reads this via python-dotenv
OPENAI_API_KEY=

# Optional: override the default model / provider
# LIDI_MODEL=openai/gpt-4o-mini

# Optional: use an existing Chromium instead of a Playwright-downloaded one
# LIDI_CHROMIUM_PATH=/usr/bin/chromium
